[pytest]
asyncio_mode = auto
testpaths = app/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers
pythonpath = .

markers =
    unit: Unit tests with mocked dependencies
    integration: Tests with real in-memory DB
    api: API endpoint tests
